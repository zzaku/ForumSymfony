{% extends 'base.html.twig' %}

{% block body %}
    <div class="category-list">
        <h1>Categories</h1>
        <ul class="category-ul">
            {% for category in categories %}
                <li class="category-li">
                    <a href="{{ path('category_boards', {'id': category.id}) }}">
                        {{ category.name }}
                    </a>
                </li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}
